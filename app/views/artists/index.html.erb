<h1> Artists </h1>

<ul>
    <% @artists.each do |artist| %>
        <li>
            <h2> <%= link_to artist.name, artist_path(artist) %> </h2>
            <p> <%= artist.song_count%> song</p>
        </li>
    <%end%>
</ul>
<hr>
